Imports System

Module Program
    Sub Main(args As String())
        Dim name(0 To 5) As String
        Dim DoB(0 To 5) As Date
        name(0) = "Bob"
        name(1) = "Danny"
        name(2) = "Lucas"
        name(3) = "Scalene"
        name(4) = "Diego"
        name(5) = "Dante"

        DoB = DateAndTime
    End Sub
End Module
